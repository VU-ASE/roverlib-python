name: producer
description: Testing a service that produces outputs

dependencies: []

outputs:
  - name: data
    address: tcp://localhost:8934
  - name: brightness
    address: tcp://localhost:8938

options:
  - name: increment
    default: 10
    type: int
